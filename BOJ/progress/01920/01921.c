#include <stdio.h>

int	main(void)
{
	int		N;
	int		M;
	int		n;

	scanf("%d", &N);
	for (int i = 0; i < N; i++)
	{
		scanf("%d", &n);
	}
	scanf("%d", &M);
	for (int i = 0; i < M; i++)
	{
		scanf("%d", &n);
	}
	return (0);
}
